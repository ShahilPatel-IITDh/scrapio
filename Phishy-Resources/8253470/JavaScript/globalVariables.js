'use strict';

let userId;
let sessionId;
let locationId;

function populateVariables(extUserid, extSessionId, extLocationId) {
    userId = extUserid;    
    sessionId = extSessionId;    
    locationId = extLocationId;
}
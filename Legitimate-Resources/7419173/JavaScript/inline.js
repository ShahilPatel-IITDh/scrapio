
          // Remove fancybox here as we're not using it, and it creates some weird tab rules
          $(document).ready(function(){
              $('#fancybox-tmp').remove();
              $('#fancybox-wrap').remove();
              $('#fancybox-loading').remove();
              $('#fancybox-overlay').remove();
          });
        